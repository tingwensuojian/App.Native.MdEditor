## [1.31.1] - 2026-06-21

### 修复
- 修复桌面窗口模式打开 Office 文件空白问题（getStandaloneAppOrigin 返回值添加尾部斜杠，解决 iframe src 端口号畸变）
- 移除不再使用的 proxy.cgi，简化入口为单一 index.cgi
- 更新打包脚本移除 proxy.cgi 引用

# 更新日志 (Changelog)

所有重要的项目变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [1.31.0] - 2026-06-17

### 新增
- 集成 ONLYOFFICE 文档编辑器（ranuts-document），支持 .docx / .xlsx / .pptx 在线编辑
- Office 编辑器嵌入模式通信协议（postMessage），包括文件打开、保存、自动保存
- 新增工具栏保存按钮启用机制（修复 SDK 因无回调 URL 禁用保存按钮）
- 新增文件切换支持：在文件树中切换 Office 文件时自动更新编辑器内容
- 新增保存状态反馈（保存成功/失败状态提示）
- 新增 Office 编辑器状态 API（/api/office/editor/status）
- 新增 Office 文件保存 API（/api/office/editor/save）

### 改进
- 优化 .doc 格式兜底处理（x2t WASM 不支持 .doc 转换时报错提示）
- 重构 Office 预览布局：打开 Office 文件时自动切换为仅预览模式
- 优化保存流程：嵌入模式下工具栏保存按钮直接回传数据至父窗口
- 优化 buffer 处理：精确切片避免 oversize backing buffer

### 文档
- 新增 Office 功能支持文档（docs/office-feature.md）

## [1.30.2] - 2026-04-09

### 修复
- 修复 PlantUML / Infographic 在高频输入与连续渲染场景下偶发不渲染的问题（渲染调度补偿与代次控制）
- 修复 Monaco 编辑器重复模块定义告警（`vs/editor/editor.main`）及 Worker 初始化告警，避免回退主线程导致卡顿

### 改进
- 优化图表渲染策略：视口优先 + 受限并发，首屏图表更快可见
- 优化 Infographic 加载兼容性与失败兜底路径，提升官网模板在当前版本下的渲染成功率
- 新增渲染耗时分段日志：区分“主流程渲染”与“图表增强渲染”，便于定位性能瓶颈
- 重型文档（Mermaid / PlantUML / Infographic）告警阈值分级，减少误报并提升调试可读性
- 增加 Infographic 持久化缓存（本地复用），重复打开同内容时渲染更快

## [1.30.0] - 2026-04-04

### 修复
- 修复 AI 对话在“翻译成英文 + 引用全文”场景下，回复结束后被误判为“主题 CSS 输出”并被后处理覆盖的问题
- 调整主题 CSS 规范化触发条件：仅在“帮我写主题”意图下执行，不再基于回复内容形态（如 `container { ... }`）自动触发

### 改进
- AI 对话后处理链路收敛，避免翻译/改写等普通任务被主题规则误处理，提升回复稳定性与可预期性

## [1.29.63] - 2026-04-04

### 新增
- 新增登录鉴权流程（Auth Bootstrap + 登录页），支持未登录状态先鉴权后进入编辑器

### 修复
- 修复“启动时显示过渡页”关闭后刷新仍短暂闪现的问题（首屏开关读取与启动阶段判定统一）
- 修复鉴权加载层与首屏过渡页叠加显示导致出现黑色“正在加载”与过渡页同时出现的问题（统一为单一过渡页链路）

### 改进
- 优化首屏过渡页时序：仅在启动阶段生效，运行中切换开关不再插播遮罩，避免先显示编辑器再被覆盖
- 调整过渡页最短停留时长与尾帧缓冲，保证主要动画可完整呈现后再退出

## [1.29.62] - 2026-03-31

### 新增
- 新增同源访问入口 `proxy.cgi`，解决桌面 iframe 场景下的跨域/鉴权/主题适配问题

### 修复
- 修复在 `proxy.cgi` 路径下静态资源（CSS/JS/MathJax/代码主题）加载 404 导致白屏的问题
- 修复 `proxy.cgi` 在用户修改安装端口后仍转发到默认端口导致 500 的问题（安装端口落盘并由代理读取）
- 修复 “新窗口打开” 在 `proxy.cgi` 场景下仍跳转到错误端口的问题（运行时读取实际监听端口）

### 改进
- 主题模式可随飞牛系统主题切换（读取并监听 `DesktopConfig-1000`）
- “关于”弹窗版本号改为构建时从 `manifest` 注入，避免与打包版本不一致
- 更新 fast/multi-arch 打包脚本：补齐 `proxy.cgi` 等运行必需文件入包，并增强路径解析/前端构建流程

---

## [1.29.61] - 2026-03-30

### 修复
- 修复 MinIO 图床“上传目录”输入异常（光标跳动/自动插入斜杠）

### 新增
- 图床配置新增 WebDAV 与自定义 OSS 类型入口

### 改进
- MinIO 配置帮助补充存储桶访问策略说明（公共读或预签名 URL），提升配置可用性
- README 文档信息细化，清理仓库中已不再维护文档的失效跳转链接

---

## [1.29.53] - 2026-03-30

### 新增
- 新增多架构打包脚本 `scripts/build-fpk-multi-arch.sh`，支持分别构建 `amd64` 与 `arm64` FPK，并按架构自动命名产物

### 改进
- 打包流程增加 ARM 原生依赖构建校验（`better-sqlite3`），降低安装后因架构不匹配导致服务启动失败的风险
- 修正 `manifest` 描述字段格式为单行 `key=value`，避免 `fnpack` 解析失败

---

## [1.29.50] - 2026-03-29

### 改进
- 设置中的 Logo 配置语义收敛为“仅影响顶部菜单栏左侧 Logo”，避免与关于弹窗 Logo 混淆
- 菜单旁 Logo 选项恢复为三种模式：`动态 SVG Logo`、`默认（Markdown）`、`自定义 URL`
- Logo 配置文案与交互优化：保留 URL 自定义输入，移除“上传本地 Logo”入口，降低配置复杂度

### 调整
- 关于弹窗 Logo 固定为动态 SVG 展示，不再跟随菜单旁 Logo 配置变更

## [1.29.36] - 2026-03-28

### 新增
- 导出配置面板新增「复古纸张」内置主题选项（`retro-paper`），支持一键切换纸张质感样式
- 自定义主题区新增「打开 AI 对话（帮我写主题）」入口，可直接拉起 AI 并进入主题生成流程

### 改进
- AI 主题生成提示词重构为“阅读体验优先”规范：覆盖排版、颜色对比、元素完整性、背景层次等基线
- AI 主题输出要求增强：强制覆盖 `container/h1-h6/p/strong/link/ul/ol/li/blockquote/codespan/code_pre/hr/image` 全部规则块
- AI 主题输出格式优化：支持内联注释，便于用户二次编辑与维护
- 主题 CSS 规范化能力增强：兼容 `.markdown/.markdown-body/#preview-area` 与子选择器映射到项目简写选择器
- 标题背景可读性约束增强：当标题使用背景色/渐变时，强制高对比文字，避免“标题文字融入背景”
- 背景应用稳定性增强：补齐 `.markdown-body` 容器级背景兜底，修复“背景仅在表格/图表可见”问题

### 修复
- 修复 AI 主题在部分场景输出代码块围栏、类选择器（如 `.h1/.container`）导致无法直接使用的问题
- 修复导出配置面板中 AI 风格/需求输入框不可编辑的问题（受控输入回调链路补齐）

## [1.29.49] - 2026-03-29

### 新增
- 图床配置新增 WebDAV 与自定义 OSS 类型入口

### 修复
- 修复 MinIO 图床“上传目录”输入异常（光标跳动、自动插入斜杠）

### 改进
- MinIO 配置帮助补充存储桶访问策略说明（公共读或预签名 URL），提升配置指引清晰度

## [1.29.35] - 2026-03-28

### 修复
- 编辑区与预览区加粗文本颜色渲染修正：预览区 `strong/b` 固定为 `#55C9E9`，避免被主题样式覆盖
- 编辑器主题改为使用 `custom-light/custom-dark`，修复仅定义 `light` 主题但实际使用 `vs/vs-dark` 导致加粗色配置不生效的问题

### 改进
- 编辑区加粗文本配色与标题色对齐（统一为标题同色）

## [1.29.34] - 2026-03-28

### 改进
- 统一弹窗动效与样式收口：所有主要对话框（含工具栏弹窗、表格、图片管理、AI 弹窗）接入一致的开/关动画与关闭链路，补齐 `prefers-reduced-motion` 降级，并将通用基础样式收敛至 `Dialog.css`，减少重复定义

## [1.29.33] - 2026-03-27

### 改进
- 植入 Mermaid/PlantUML/Infographic 示例区块到默认文档
- PlantUML 字体颜色自适应与 Infographic 逻辑对齐：按主题背景/明暗模式选择对比字色，并同步支持背景色回退
- PlantUML 渲染缓存键升级，纳入主题/背景/阈值，避免主题切换后命中旧颜色
- 导出与预览链路统一传递字体对比参数，确保渲染一致

## [1.29.32] - 2026-03-27

### 改进
- PlantUML 字体颜色自适应与 Infographic 逻辑对齐：按主题背景/明暗模式选择对比字色，并同步支持背景色回退
- PlantUML 渲染缓存键升级，纳入主题/背景/阈值，避免主题切换后命中旧颜色
- 导出与预览链路统一传递字体对比参数，确保渲染一致

## [1.29.31] - 2026-03-27

### 改进
- Infographic 文本颜色策略重构：未配置主题背景时按明暗模式回退（亮模深字、暗模浅字）；配置背景色时按背景亮度自动选择高对比字体色（浅背景深字、深背景浅字）
- Infographic 渲染参数打通：文本对比阈值支持配置（`exportConfig.infographicContrastThreshold`），并在渲染链路中统一传递
- 主题切换联动增强：主题模式、背景色、对比阈值变更会触发 Infographic 重新渲染，避免旧颜色残留

### 修复
- 修复 Infographic 字体颜色在主题切换后未及时更新的问题（补全渲染依赖与触发条件）
- 修复 Infographic 颜色缓存命中错误导致“规则已改但显示未变化”的问题（缓存键升级并纳入背景色/阈值等关键参数）

## [1.29.30] - 2026-03-27

### 新增
- 预览新增 `infographic` fenced 代码块渲染能力（AntV Infographic），支持 DSL 识别、节点替换与异步渲染
- Infographic 渲染支持图标资源加载器（Iconify），`icon mdi/...` 等资源可在预览中拉取并渲染

### 改进
- Infographic 渲染流程新增事件驱动等待（`rendered/error`）与超时保护，避免 “Infographic is not rendered yet” 时序问题
- Infographic 渲染错误信息可读性增强：支持数组/对象错误展开，输出 `message/reason`，便于定位 DSL 语法问题
- 预览图形容器样式统一（Mermaid/PlantUML/Infographic）：SVG 自适应宽度，修复因固定宽度导致的横向滚动条

### 已知限制
- 当前依赖版本（`@antv/infographic@0.2.16`）与官网 Gallery 模板并非完全一致，部分模板 ID（如 `chart-column-grouped-simple`）在本版本不可用
- `infographic` fenced 代码块当前仅解析 DSL，不直接执行 JS 示例代码（`import/new Infographic(...)`）

## [1.29.29] - 2026-03-27

### 改进
- PlantUML 渲染链路改为前端请求后端 `/api/plantuml/svg` 代理，规避浏览器直连官方服务的 CORS 限制
- PlantUML 代码块解析兼容性增强：放宽 HTML 匹配规则并增加 DOM 二次兜底替换，提升不同渲染结构下的命中率
- PlantUML 预览渲染新增按代码内容缓存，重复图表复用 SVG，减少重复请求与渲染耗时
- 导出一致性增强：HTML、微信公众号与图片导出前统一确保 PlantUML 已渲染，避免“预览有图、导出缺图”

### 修复
- 修复 PlantUML 在部分环境中“请求无报错但未渲染为 SVG”的问题
- 修复 PlantUML 在导出链路中偶发保留源码块而非图形的问题

## [1.29.28] - 2026-03-27

### 新增
- 图床配置界面新增「按年月日自动分目录」开关，位置在“上传目录”下方，支持添加与编辑时独立配置

### 改进
- 图床按类型独立控制日期目录策略：GitHub、七牛云、阿里云 OSS、腾讯云 COS、本地存储统一支持 `useDatePath`（默认开启）
- 本地存储适配器支持关闭日期分目录后直接存储到根目录，并保持列表/读取/删除链路兼容

### 修复
- GitHub 在关闭日期分目录时，上传路径不再出现前导斜杠（根路径上传兼容）

## [1.29.27] - 2026-03-27

### 改进
- 统一字体来源：设置与导出配置改为同源字体数据，确保选项来源一致
- 导出配置字体扩展为与设置同一完整字体集合，并保持字体预览一致
- 菜单交互优化：视图菜单中的“放大/缩小”和“页面布局”项点击后保持菜单展开，便于连续调整
- 缩放设置持久化：菜单“放大/缩小/重置缩放”操作新增持久化保存到数据库
- 移动端紧凑菜单支持分组折叠，分类项默认折叠，便于小屏下快速定位功能
- 移动端紧凑菜单中“最近文件”“导出”子项支持独立折叠，默认关闭
- 字体下载链路新增后端本地缓存能力，支持跨浏览器复用本地字体缓存
- 图床上传路径策略统一：GitHub、七牛云、阿里云 OSS、腾讯云 COS、本地存储支持按“上传目录 + 年/月/日”组织文件
- 图床配置新增“按年月日自动分目录”开关（默认开启），并支持在添加/编辑图床时独立配置
- 本地存储适配日期子目录结构：图片读取、列表递归扫描与删除链路统一支持多级路径

### 修复
- 修复导出配置选择中文字体（如“霞鹜文楷”）时预览区未正确应用字体栈的问题
- 修复设置与导出配置中字体下载按钮尺寸不一致的问题（统一为 24x24）
- 修复移动端菜单折叠改造后 `MenuBar.jsx` JSX 闭合错误导致的编译失败
- 修复七牛云“上传目录”配置在特定输入（如仅 `/`、前后多余斜杠）下不生效的问题，统一规范化为可预期路径
- 图床删除链路增强：GitHub/七牛/阿里/腾讯/本地删除图片后支持逐级清理空目录（含目录占位对象）

### 调整
- 去除字体下拉中的“云字体需下载”文字标注，保留下载按钮与进度反馈

## [1.29.26] - 2026-03-26

### 改进
- 文件打开布局规则统一：图片格式打开后强制仅预览，非 Markdown 文本打开后强制仅编辑，Markdown 打开默认左右布局
- 收藏夹面板增强：展开/收起状态持久化；拖拽与上下移动调整为“本地即时更新 + 防抖持久化”以减少频繁写入

### 修复
- 文件属性对话框：名称/路径复制在受限运行环境下增加手动复制兜底，避免点击无反馈
- 对话框样式统一：修复重命名/导出/设置与文件属性之间按钮色值与容器样式不一致的问题

### 调整
- 另存为：文件名后缀由固定 `.md` 调整为“默认 `.md`，可手动改为其他后缀”
- 另存为：新增文件名校验（非法字符、分隔符、结尾点号）

## [1.29.22] - 2026-03-18

### 改进
- 图床设置：阿里 OSS、腾讯云、Custom 图标与明暗模式统一
- 图床设置：删除操作改为内置风格确认弹窗
- 图床设置：列表不再使用内部滚动，直接完整展示
- 图床管理：删除图床时同步清理关联图片记录，避免删除失败
 
## [1.29.24] - 2026-03-25

### 修复
- PDF 预览：首次打开稳定，避免二次自动渲染导致抖动/空白（移动端与桌面端）
- PDF 导出/打印：移动端 `window.open` 被拦截时不再崩溃（对 `document.write` 做健壮性保护）
- 剪贴板兼容：非 HTTPS/移动端缺少 `Clipboard API` 或 `ClipboardItem` 时提供降级 shim，避免编辑器 API 直接报错

### 改进
- 开发模式下减少 PDF 预览的重复加载触发（移除 `React.StrictMode` 以降低 dev 行为差异）

## [1.29.25] - 2026-03-26

### 新增
- Office 只读预览：支持 `.docx/.xlsx/.pptx` 专用预览页（禁用编辑区与 Markdown 预览区）
- DOCX 预览：后端提取纯文本并展示（超大内容截断提示）
- XLSX 预览：纯 HTML 表格展示，支持多 Sheet 切换
- XLSX 行懒加载：同一页内滚动到底部自动加载更多行（无分页按钮）
- PPTX：实验性降级提示（暂不解析预览）

### 安全
- Office ZIP 炸弹防护：限制解压体积/条目数/压缩比并检测宏文件（仅提示，不执行）

## [1.29.0] - 2026-03-17

### 新增
- GitHub 图床多仓库支持：配置界面通过添加按钮管理多个仓库名，上传时自动轮询
- 图片库多图床标签页：顶部标签页根据已配置图床动态生成，自动选中默认图床
- 云端图床图片本地缩略图缓存：首次加载后缓存元数据至数据库，后续打开无需重复请求云端
- 缩略图按图床 ID 分目录存储（`{TRIM_PKGVAR}/thumbnails/{bedId}_{name}/`），按需创建
- CDN 加速域名选项新增 jsDelivr，移除 ghproxy/fastgit 不稳定选项
- GitHub 图床配置说明折叠面板（默认收起）
- 图片库切换图床时显示加载动画

### 改进
- 图片库刷新按钮触发强制从云端拉取，普通切换使用本地缓存
- 删除图床时同步清理对应缩略图目录和数据库缓存
- 删除图床图片时同步删除本地缩略图和缓存记录
- hot-update-full.sh 修复后端路径（`target/server/` 而非 `target/app/server/`）
- DELETE 请求 body 正确解析，修复删除图床图片 400 错误
- 图床设置列表和类型选择按钮正确适配暗黑模式
- 图片库标签页支持横向滚动，适应更多图床
- 图片库 header 两行布局：标签行带背景色，操作行右对齐
- getAllConfigs 返回解密后的完整 config 字段，编辑图床时正确回填已有配置

### 修复
- 修复切换 CDN 域名后上传图片 URL 不正确的问题（adapter 缓存未清除/config 解密问题）
- 修复加载中图标不旋转的问题

---

## [1.28.11] - 2026-03-16

### 改进
- 默认构建 fpk：`bash build-and-deploy.sh` 现默认执行 `build-optimized.sh`（优化打包 + 安装 + 重启）
- 开发文档：快速开发部署说明、构建部署规则中补充默认 fpk 构建命令说明

---

## [1.28.10] - 2026-03-16

### 新增
- 文件树关闭动画：面板宽度收缩，主内容区联动扩展，无中间空白
- 导出配置面板：关闭动画与主内容区联动（桌面端、移动端）

### 改进
- 显示行号、自动换行、字体设置持久化并与设置对话框同步
- 内置字体：使用 @fontsource/jetbrains-mono、@fontsource/fira-code，离线可用
- 移动端菜单：下拉位置根据视口计算，宽度与滚动条优化

### 修复
- 修复 handleCloseFileTree 在初始化前被引用导致的 ReferenceError（调整定义顺序）

---

## [1.28.9] - 2026-03-16

### 改进
- 文件树根目录：正确显示应用设置中用户授权的文件夹（TRIM_DATA_ACCESSIBLE_PATHS）
- 文件树：隐藏 mdeditor 目录本身，仅显示其子目录（Folder、新建文件夹等）
- 空白处新建文件夹：固定创建在 mdeditor 下，新建后正确刷新并显示
- API `/api/files?path=/` 增加 `createFolderRoot` 字段，供前端根级新建使用

### 修复
- 修复应用设置中添加授权文件夹后文件树不显示的问题
- 修复 mdeditor 过滤后根级新建文件夹失败的问题
- 修复 Folder 被重复挂载显示的问题

---

## [1.28.8] - 2026-03-15

### 新增
- AI 文生图移动端布局：固定顶部栏、核心预览区、提示词+生成区、可折叠高级设置（模型/尺寸/种子/数量/参考图）
- AI 配置面板移动端适配：侧边栏改为顶部下拉选择服务商，API 配置与模型列表可折叠，关键按钮固定底部

### 改进
- AI 配置面板：移动端隐藏顶部搜索栏，表单单列布局、输入框/按钮触控区域放大至 44px+
- AI 配置面板：移动端保存/重置按钮全宽、最小高度 48px，便于操作

---

## [1.28.7] - 2026-03-15

### 新增
- AI 文生图尺寸选项增加比例图标（正方形/横版/竖版），直观展示各尺寸样式
- 参考图、图片数量、种子三处增加灰色提示文案，说明支持范围与用法

### 改进
- 文生图 endpoint 选择逻辑修复：选择非 builtin 服务商（硅基流动、OpenAI 等）时不再误用内置代理
- Fal Kolors 支持多图生成，传递 `num_images` 参数（1–8 张）

### 修复
- 修复切换文生图服务商时 endpoint 未同步更新导致仍走 proxy-ai.doocs.org 的问题

---

## [1.28.6] - 2026-03-15

### 新增
- AI 配置：模型标签页（对话/图片）移至顶部，API Key 共用，代理地址按 tab 区分
- 图片 tab 支持独立代理地址配置（imageConfig.endpoints），默认使用对话代理地址
- 连通性检查随 tab 切换：对话 tab 测对话 API，图片 tab 测图片 API，模型选项自动切换
- 模型列表行内连通性检查：结果以浮窗显示在试管按钮旁，不占用上方通知区

### 改进
- 保存/重置按钮固定在右侧面板底部，左侧服务列表贯穿全高
- 点击模型「使用」开关后立即持久化到数据库，无需再点保存
- 连通性检查通过后不再自动启用模型，由用户自行在列表中点击使用
- 主标签按钮布局参考底部操作区，未选中状态也显示边框
- 图片模式下，服务商无图片模型时显示「服务商暂无图片相关模型」

---

## [1.28.5] - 2026-03-15

### 新增
- AI 对话「引用全文」启用时，输入区显示引用框，与消息中的引用块风格一致

### 改进
- AI 对话引用功能改为只同时引用一个：快捷指令或 AI 回复引用会替换已有引用，启用引用全文会清空其他引用块
- 引用全文块仅显示「引用全文」四字，悬停显示内容预览
- getEditorContent 回退逻辑优化，确保在编辑器未挂载时仍能正确获取 content 状态

---

## [1.28.4] - 2026-03-15

### 新增
- AI 助手按钮支持浮动拖动，可自由调整位置，位置持久化保存

### 改进
- 文生图模型选择与 AI 对话区逻辑统一：展示所有已启用服务商、浮框+搜索+分组、选择时同步更新服务商与尺寸
- 模型切换列表分类名称增加背景色区分（AI 对话与文生图一致）

---

## [1.28.3] - 2026-03-15

### 新增
- 导出配置主题（如「渐变背景」）持久化，下次打开应用时会恢复上次选择

### 修复
- 修复打开应用时预览区图注不显示的时序问题（图注更新在 Markdown 渲染完成前执行导致）

---

## [1.28.2] - 2026-03-15

### 新增
- 安装向导支持用户自定义应用端口，安装前自动检查端口占用

### 改进
- 应用入口改为 iframe 模式，在飞牛桌面内打开
- 发布按钮从菜单栏移至工具栏右侧（导出配置与保存之间）
- 非飞牛桌面下点击发布直接打开 SyncDialog，由弹窗内部检测扩展并显示（检测中/未安装引导/平台选择），不再使用 toast 提示

### 修复
- manifest 设置 checkport=false，端口检查由 install_init 负责，支持用户自定义端口

---

## [1.28.1] - 2026-03-14

### 新增
- AI 对话快捷指令「帮我写主题」，根据自定义 CSS 主题规范生成主题代码
- 快捷指令改为引用块插入，提示词不写入编辑区，便于查看与补充

### 改进
- 文生图生成后自动保存到图片库（不压缩），插入时使用本地路径
- 插入图片/链接到文档时，上下各留一行空格
- 链接按钮改为插入 `[链接](https://)` 模板
- 上传图片、粘贴、拖拽插入时，上下各留一行空格

### 修复
- 修复 API 返回 500 或空响应时的 `Unexpected end of JSON input` 崩溃，新增 `safeParseJsonResponse` 安全解析
- 修复 FileTree `onSelectStart` 非标准事件导致的 React 警告，改用 CSS `user-select: none`

---

## [1.27.32] - 2026-03-12

### 新增
- 新增全局复用的 `AnimatedSelect` 组件，统一导出配置、设置和图片管理中的下拉交互风格
- 新增默认 Markdown 展示文档常量，空白文档模板会自动带出演示内容

### 改进
- 统一导出配置、设置、图片管理三处下拉的尺寸、间距、箭头和选中态视觉
- 调整导出配置面板中的自定义主题管理区布局与按钮顺序，提升窄面板下的可用性

### 修复
- 修复切换文件时未提示保存导致内容丢失的问题
- 修复多浏览器标签页刷新后页面主题与 Monaco 编辑区主题可能错配的问题
- 修复导出配置面板按钮与下拉在拉伸场景下的尺寸不一致问题

---

## [1.27.6] - 2026-03-08

### 新增
- 完善微信公众号导出功能
- Mermaid 流程图自动转换为 SVG Data URL 图片
- 数学公式使用 CodeCogs API 在线渲染为高清 PNG（300 DPI）

### 改进
- 从 KaTeX annotation 标签提取原始 LaTeX 代码
- 图片缩放：百分比宽度自动转换为像素值（基于微信公众号 677px 标准容器）
- 优化导出流程，支持异步处理
- 添加详细的调试日志

### 修复
- 修复图片样式处理逻辑
- 修复数学公式在微信编辑器中显示不清晰的问题

---

## [1.27.5] - 2026-03-08

### 改进
- 优化热更新功能

---

## [1.27.4] - 2026-03-08

### 修复
- 修复微信公众号格式导出时图片缩放样式丢失问题
- 将百分比宽度转换为像素值以兼容微信编辑器

---

## [1.27.3] - 之前版本

### 新增
- 完善 HEIC/HEIF 支持
- 使用 heic-convert 库实现可靠转换
- 自动跳过前端压缩
- 优化用户体验

---

## [1.27.0] - 之前版本

### 新增
- 热更新功能
  - 前端热更新（hot-update-frontend.sh）
  - 后端热更新（hot-update-backend.sh）
  - 完整热更新（hot-update-full.sh）
  - 监听模式（hot-update-watch.sh）
- 热更新文档和快速参考

### 改进
- 优化构建流程
- 添加自动权限修复

---

## [1.26.x] - 之前版本

### 新增
- 图片管理功能
- 图片压缩功能
- 图注格式支持

### 改进
- 优化编辑器性能
- 改进文件树显示

---

## [1.25.x] - 之前版本

### 新增
- Mermaid 流程图支持
- KaTeX 数学公式支持
- 代码高亮功能

### 改进
- 优化 Markdown 渲染
- 改进预览同步

---

## [1.20.x] - 之前版本

### 新增
- 文件历史功能
- 版本管理
- 自动保存功能

### 改进
- 优化文件操作
- 改进用户界面

---

## [1.15.x] - 之前版本

### 新增
- Monaco Editor 集成
- 语法高亮
- 代码折叠

### 改进
- 优化编辑体验
- 改进快捷键支持

---

## [1.10.x] - 之前版本

### 新增
- 基础 Markdown 编辑功能
- 实时预览
- 文件树管理

### 改进
- 优化界面布局
- 改进响应速度

---

## [1.0.0] - 初始版本

### 新增
- 基础 Markdown 编辑器
- 文件读写功能
- 简单预览功能

---

## 图例说明

- **新增**：新功能
- **改进**：对现有功能的改进
- **修复**：Bug 修复
- **移除**：移除的功能
- **安全**：安全相关的修复
- **废弃**：即将移除的功能

---

## 版本号说明

版本格式：`主版本号.次版本号.修订号`

- **主版本号**：不兼容的 API 修改
- **次版本号**：向下兼容的功能性新增
- **修订号**：向下兼容的问题修正

---

*最后更新：2026-04-09（1.30.2）*
